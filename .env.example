# Vibe-Astock —— 环境变量模板
#
# LLM 走 MiMo，凭据默认从 ~/.config/mimo/mimo.env 自动加载（不需要在这里重复填）。
# 只有当你想覆盖默认时才在下面设置：
#
# MIMO_API_KEY=...            # 切勿提交真实 key
# MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1
# MIMO_MODEL=mimo-v2.5-pro    # deep 档模型；quick 档写死 mimo-v2.5
#
# Agent-Reach（题材热点分析师用于印证题材的可选外部工具，走 mcporter/exa）：
# 不装也能跑 —— 题材分析师会退回只用涨停原因题材串，并在报告里说明少了这一路印证。
# 情绪面真实数据需要 akshare（pip install akshare）。
#
# A 股全量静态列表（启动只读本地缓存；在「数据管理」页手动刷新）：
# 刷新时的网络源顺序（逗号分隔，首个成功即停止，不写 akshare 则不走 AkShare）：
# STOCK_LIST_SOURCES=eastmoney,akshare   # 可选 eastmoney / akshare
#
# 涨停原因题材串：默认走同花顺涨停池（免费，与 zvt LimitUpInfo 同源）。
# 失败时的备用 —— pywencai（需本机 Node.js 16+，pip install pywencai）：
# IWENCAI_COOKIE=从问财网页请求头复制的 Cookie
# 再失败可走官方问财 openapi：
# IWENCAI_API_KEY=你的key
